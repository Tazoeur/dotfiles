# todo:
# brew
# spotify
# vscode
# alacritty
# discord
# telegram
# bat (not with apt!!)


- apt:
    - build-essential
    - libssl-dev
    - curl
    - wget
    - vim
    - neovim
    - vlc
    - fzf
    - ripgrep
    - tmux

- shell:
    - [nvm install 16, Installing node 16 (nvm)]
    - [nvm use 16, Use node 16 (nvm)]
# brew install gh
